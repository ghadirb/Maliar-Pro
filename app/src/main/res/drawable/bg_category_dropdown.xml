<?xml version="1.0" encoding="utf-8"?>
<!--
    Background for the "دسته‌بندی" (category) AutoCompleteTextView popup.

    Previously this popup had no explicit background/elevation, so on some
    devices/themes it rendered with very low contrast against whatever was
    behind the dialog, making the category titles hard to see, and the list
    had no visual separation from the rest of the screen. This gives it a
    solid, theme-aware (day/night) surface color with rounded corners so it
    always reads clearly, matching the app's card style.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/card_background" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/divider" />
</shape>
